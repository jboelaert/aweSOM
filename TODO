## BUGS
* Crash when only one variable selected
* Crash when duplicate rownames in data import (when rownames checked)
* Fix rownames

## TODO
* greyscale color
* camemberts : ajouter % dans le texte
* Boxplot : highlight doesn't work for other cells
* Help messages
* Data file error messages
* hex grid on pca initialization ?
* Hitmap : mouse focus cell and polygon
* Lines : changer le mécanisme de proximité
* check that sqrt(weights) where weights sum to one is correct
* serveur Strasbourg
* barplot categorical
* Third contrast option : no normalization
* Star categorical realValue (ex : frondeur 1.62)
* Barplot hex norm : 0-1
* SVG save
* Warning une seule colonne csv
* File locale
* save R session
* isolate train in graph var choice
* isolate ok.data() in graph panel
* Starplot : n'importe quoi, parfois les ~0 sont à l'exterieur des étoiles, problème grille 1D, et à cacher quand 0 obs
* Names plot : enlever ?
* Line plot empty cell : enlever cercles

## Wishlist
* Internationalization
* choices for graphs: median, prototypes
* U-matrix distance
* Polygon distance
* scatterplot (2 var) par cellule, échelle commune
* Distance of cells to active cell
* grid on the plots
* légende (ggplot à part ?)
* save to html
* mixScale
* call from R : kohonen, sombrero, ...
* Relational SOM
* Additional variables : 2 kinds
* Variable names plot (wordcloud of variables that characterize cell)
* data formats (sas, stata..., xls ?)
* onglet "code" en générant le code reproductible
* sur camemberts : texte indiquant le nom des observations de la part sous focus

